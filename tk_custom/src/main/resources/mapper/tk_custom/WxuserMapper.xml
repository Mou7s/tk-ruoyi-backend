<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.tk_custom.mapper.WxuserMapper">
    
    <resultMap type="Wxuser" id="WxuserResult">
        <result property="userid"    column="userid"    />
        <result property="name"    column="name"    />
        <result property="dept"    column="dept"    />
        <result property="position"    column="position"    />
    </resultMap>

    <sql id="selectWxuserVo">
        select userid, name, dept, position from wxuser
    </sql>

    <select id="selectWxuserList" parameterType="Wxuser" resultMap="WxuserResult">
        <include refid="selectWxuserVo"/>
        <where>  
            <if test="name != null  and name != ''"> and name like concat('%', #{name}, '%')</if>
            <if test="dept != null  and dept != ''"> and dept = #{dept}</if>
            <if test="position != null  and position != ''"> and position = #{position}</if>
        </where>
    </select>
    
    <select id="selectWxuserByUserid" parameterType="String" resultMap="WxuserResult">
        <include refid="selectWxuserVo"/>
        where userid = #{userid}
    </select>

    <insert id="insertWxuser" parameterType="Wxuser">
        insert into wxuser
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="userid != null">userid,</if>
            <if test="name != null">name,</if>
            <if test="dept != null">dept,</if>
            <if test="position != null">position,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="userid != null">#{userid},</if>
            <if test="name != null">#{name},</if>
            <if test="dept != null">#{dept},</if>
            <if test="position != null">#{position},</if>
         </trim>
    </insert>

    <update id="updateWxuser" parameterType="Wxuser">
        update wxuser
        <trim prefix="SET" suffixOverrides=",">
            <if test="name != null">name = #{name},</if>
            <if test="dept != null">dept = #{dept},</if>
            <if test="position != null">position = #{position},</if>
        </trim>
        where userid = #{userid}
    </update>

    <delete id="deleteWxuserByUserid" parameterType="String">
        delete from wxuser where userid = #{userid}
    </delete>

    <delete id="deleteWxuserByUserids" parameterType="String">
        delete from wxuser where userid in 
        <foreach item="userid" collection="array" open="(" separator="," close=")">
            #{userid}
        </foreach>
    </delete>
</mapper>